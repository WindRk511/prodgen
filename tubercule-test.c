/**@file tubercule.c
* @brief cette programme qui stock les information basique des tubercules
* @author Windrick
*/
#include prod.h
#include<stdio.h>
#include<stdlib.h>
#include<string.h>

/** @fn cette fonction qui stock le donnée de tubercule
* @param void
* @return prod*
*/
prod* tubercule()
{
prod* tubercule()
{
        int NBR;
        NBR=1;
        prod* tub = malloc(NBR*sizeof(prod));

	strcy(tub->nom,Banana);
	tub[0].type=3;
	tub[0].sol[0]=3;
	tub[0].sol[1]=2;
	tub[0].sais[0]=0;
	tub[0].mois[0]=1;
	tub[0].mois[1]=2;
	tub[0].mois[2]=3;

}
